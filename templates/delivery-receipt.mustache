<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <style>
    @page { size: A4; margin: 12mm 0 10mm 0; }
    html, body {
      height: 97%;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      color: #111;
      padding: 0 14mm;
    }
    .logo-absolute {
      position: absolute;
      left: 13%;
      top: 0;
      width: 50px;
      z-index: 10;
    }
    .company {
      font-size: 11px;
      line-height: 1.25;
      margin-bottom: 6px;
      color: #111;
      display: flex;
      justify-content: center;
      align-items: start;
      position: relative;
    }
    .company .info {
      text-align: center;
      width: 100%;
    }
    .company .info .name {
      font-weight: 700;
      font-size: 20px;
      margin-left: 10px;
    }
    .date {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      font-size: 12px;
    }
    .invoice-header-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin: 20px 0;
    }
    .invoice-header-title {
      flex: 1;
      text-align: center;
      margin: 0;
      font-size: 19px;
      font-weight: 700;
    }
    .invoice-header-no {
      flex: 1;
      text-align: right;
      color: red;
      font-size: 12px;
      font-weight: 700;
    }
    .invoice-header-empty {
      flex: 1;
      font-size: 12px;
    }
    .grid2 {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 4px 22px;
      margin: 10px 0;
      font-size: 12px;
    }
    .kv {
      display: flex;
      font-weight: 700;
    }
    .kv .k {
      color: #111;
      white-space: nowrap;
      font-weight: 700;
    }
    .k {
      white-space: nowrap;
      font-weight: 700;
    }
    .underline {
      border-bottom: 1px solid #111;
      width: 100%;
      padding-left: 6px;
      font-weight: 400;
      display: inline-block;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 12px;
    }
    .items th, .items td {
      border: 1px solid #111;
      padding: 6px;
      vertical-align: top;
    }
    .items th {
      text-align: left;
      font-weight: 700;
    }
    .items .qty,
    .items .unit {
      width: 60px;
      text-align: center;
      vertical-align: middle;
    }
    .items .particulars {
      width: auto;
      text-align: center;
    }
    .part {
      line-height: 1.35;
      text-align: left;
      padding-left: 10px;
    }
    /* Pick-up/Delivery header blocks */
    .pd-header-row {
      display: flex;
      flex-direction: row;
      gap: 0;
    }
    .pd-header-block {
      flex: 1 1 0;
      padding: 10px;
      vertical-align: top;
    }
    .pd-header-content {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .pd-header-row .pd-header-block + .pd-header-block {
      border-left: 1px solid #111;
    }
    .pd-header-line {
      display: flex;
      align-items: center;
    }
    .pd-header-label {
      white-space: nowrap;
    }
    .pickup-block, .delivery-block {
      display: flex;
      flex-direction: column;
      gap: 6px;
      padding: 10px;
    }
    .pickup-row, .delivery-row {
      display: flex;
      align-items: center;
      gap: 8px;
    } 
  </style>
</head>
<body>
  <div>
    <div class="company">
      {{#logoSrc}}
        <img src="{{logoSrc}}" class="logo-absolute" />
      {{/logoSrc}}
      <div class="info">
        <div class="name">MONARCH DIVERSIFIED LOGISTICS, INC.</div>
        <div>Lot 1 Ninoy Aquino Ave. cor. Old Kabihasnan St. San Dionisio,</div>
        <div>City of Parañaque, NCR, Fourth District, Philippines 1700</div>
        <div>Tel. No.: (02) 4523644 / 8691-0165</div>
        <div>VAT Reg. TIN:009-586-612-00000</div>
      </div>
    </div>

    <div class="invoice-header-row">
      <div class="invoice-header-empty"></div>
      <div class="invoice-header-title">DELIVERY RECEIPT</div>
      <div class="invoice-header-no">{{drNumber}}</div>
    </div>

    <div class="grid2">
      <div class="kv">
        <div class="k">Client:</div><div class="underline">{{client}}</div>
      </div>
      <div class="kv">
        <div class="k">Date:</div><div class="underline">{{date}}</div>
      </div>
      <div class="kv">
        <div class="k">Shipment Reference:</div><div class="underline">{{shipmentReference}}</div>
      </div>
      <div class="kv">
        <div class="k">File#:</div><div class="underline">{{transactionFile}}</div>
      </div>
    </div>

    <table class="items" style="margin-top:10px;">
      <thead>
        <tr>
          <th colspan="3" class="pickup-header">
            <div class="pickup-block">
              <div class="pickup-row">
                <span class="k">Pick-up Site:</span>
                <span class="underline">{{pickupSite}}</span>
              </div>
              <div class="pickup-row">
                <span class="k">Pick-up Address:</span>
                <span class="underline">{{pickupAddress}}</span>
              </div>
              <div class="pickup-row">
                <span class="k">Contact Person:</span>
                <span class="underline">{{pickupContactPerson}}</span>
              </div>
              <div class="pickup-row">
                <span class="k">Contact Number:</span>
                <span class="underline">{{pickupContactNumber}}</span>
              </div>
            </div>
          </th>
          <th colspan="3" class="delivery-header">
            <div class="delivery-block">
              <div class="delivery-row">
                <span class="k">Delivery Site:</span>
                <span class="underline">{{deliverySite}}</span>
              </div>
              <div class="delivery-row">
                <span class="k">Delivery Address:</span>
                <span class="underline">{{deliveryAddress}}</span>
              </div>
              <div class="delivery-row">
                <span class="k">Contact Person:</span>
                <span class="underline">{{deliveryContactPerson}}</span>
              </div>
              <div class="delivery-row">
                <span class="k">Contact Number:</span>
                <span class="underline">{{deliveryContactNumber}}</span>
              </div>
            </div>
          </th>
        </tr>
        <tr>
          <th class="qty">QTY.</th>
          <th class="unit">UNIT</th>
          <th class="particulars" colspan="3">PARTICULARS</th>
        </tr>
      </thead>
      <tbody>
        {{#items}}
        <tr>
          <td class="qty">{{qty}}</td>
          <td class="unit">{{unit}}</td>
          <td class="particulars" colspan="3">
            <div class="part">
              <div><b>Description:</b> {{description}}</div>
              <div><b>Lot/Batch No.:</b> {{lot}}</div>
              <div><b>Expiry Date:</b> {{expiryDate}}</div>
              <div><b>Serial No.:</b> {{serialNo}}</div>
            </div>
          </td>
        </tr>
        {{/items}}
      </tbody>
    </table>
  </div>
</body>
</html>